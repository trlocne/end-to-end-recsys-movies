apiVersion: v1
kind: Service
metadata:
  name: {{ include "recsys-ui.fullname" . }}
  labels:
    {{- include "recsys-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "recsys-ui.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
